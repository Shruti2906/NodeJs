//divide/split your code into diffrent modules.
    // function add(a, b){
    //     return a*b;
    // }
console.log(add(10, 20));

// so instead of using it here, we'll create one file math.js and define this function add in that file.
